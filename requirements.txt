streamlit       # Streamlit framework for the web app interface
spacy      # For NLP and concept extraction
transformers     # For using pre-trained AI models (e.g., BERT)
networkx        # For mind map graph visualization
matplotlib        # To help visualize graphs if needed
Pillow        # For image export capabilities (PNG, etc.)
pdfplumber        # For PDF file extraction
PyMuPDF        # Additional library for handling PDFs
pydub        # For handling audio files (if audio extraction is needed)
scikit-learn       # Useful for any machine learning-related tasks
numpy            # Supporting library for numerical processing
